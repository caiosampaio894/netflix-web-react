import styled from 'styled-components'

export const ErrorDiscription = styled.p`
    text-align: center;
    font-weight: bold;
    font-size: 13px;
    color: ${props => props.theme.pallete.core.primary};
`